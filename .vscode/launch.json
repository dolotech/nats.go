{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceRoot}/cmd/signal",
      "args": [
        "--conf=config/cfg.toml"
      ],
      "output": "${workspaceRoot}/bin/signal",
      "cwd": "${workspaceRoot}"
    }
  ]
}